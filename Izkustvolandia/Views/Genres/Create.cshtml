@using Izkustvolandia.Domain
@model Izkustvolandia.Models.GenreViewModel

@{
    ViewData["Title"] = "Създай жанр";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="container my-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow-sm border-0">
                <div class="card-header text-white text-center">
                    <h3 class="mb-0">🎨 Създай нов жанр</h3>
                </div>
                <div class="card-body">
                    <form asp-action="Create" enctype="multipart/form-data">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="mb-3">
                            <label asp-for="Name" class="form-label fw-bold">Име на жанра</label>
                            <input asp-for="Name" class="form-control" placeholder="напр. Импресионизъм" />
                            <span asp-validation-for="Name" class="text-danger small"></span>
                        </div>

                        <div class="d-flex justify-content-between align-items-center mt-4">
                            <a asp-action="All" class="btn btn-outline-secondary">
                                <i class="bi bi-arrow-left-circle"></i> Назад към списъка
                            </a>
                            <button type="submit" class="btn btn-success">
                                <i class="bi bi-check-circle"></i> Създай
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>

<script>
    $(document).ready(function() {
        $(".js-example-basic-multiple").select2(); // Initialize Select2 if used
    });
</script>
