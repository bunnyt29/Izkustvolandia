@using Microsoft.AspNetCore.Mvc.TagHelpers
@model IEnumerable<Izkustvolandia.Models.GenreViewModel>

@{
    ViewData["Title"] = "Жанрове";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="container my-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold text-primary">🎨 Жанрове</h2>

        @if (User.IsInRole("Admin"))
        {
            <a asp-action="Create" class="btn btn-success">
                <i class="bi bi-plus-lg"></i> Добави нов жанр
            </a>
        }
    </div>

    <div class="table-responsive shadow-sm border rounded">
        <table class="table table-hover table-striped align-middle mb-0">
            <thead class="table-dark">
            <tr>
                <th>Име</th>
                <th class="text-end">Действия</th>
            </tr>
            </thead>
            <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                    <td class="text-end">
                        <a asp-action="Edit" asp-route-id="@item.GenreId" class="btn btn-sm btn-outline-primary me-2">
                            <i class="bi bi-pencil-square"></i> Редактирай
                        </a>
                        <a asp-action="Delete" asp-route-id="@item.GenreId" class="btn btn-sm btn-outline-danger">
                            <i class="bi bi-trash"></i> Изтрий
                        </a>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</section>